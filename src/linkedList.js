const { SingleLinkedList } = require("./lists/singleLinkedList")
const { DoubleLinkedList } = require("./lists/doubleLinkedList")
const { Node, SingleNode, DoubleNode } = require("./nodes/nodes")

module.exports = {
    SingleLinkedList, DoubleLinkedList, Node, SingleNode, DoubleNode
}